import React from 'react';
import { object } from 'prop-types';
import './Header.css';

const Header = () => {
  return (
    <div className='header'>
      Header
    </div>
  );
};

export default Header;