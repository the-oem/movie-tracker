import React from 'react';
import { saveToCache, getFromCache } from './storageUtils';

describe('storageUtils test', () => {
  it('should save to cache', () => {
    const ls = global.localStorage;
  });

  // it('should...', () => {})
  // it('should...', () => {})
  // it('should...', () => {})
  // it('should...', () => {})
});
